describe('User model', () => {
    it('should find a created user in the list of users', () => {

    })
    it('should find a created user in the list of users', () => {
        
    })

    it('should find a created user in the list of users', () => {
        
    })

    it('should find a created user in the list of users', () => {
        
    })

    it('should find a created user in the list of users', () => {
        
    })
});